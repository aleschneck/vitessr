# vitessr
server side rendering with Vite

## Getting Started

Install dependencies
* npm
  ```sh
  npm i
  ```

Build
* npm
  ```sh
  npm run build
  ```

Run server
* npm
  ```sh
  npm run server
  ```
